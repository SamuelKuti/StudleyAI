export interface FAQItem {
    question: string;
    answer: string;
  }
  
  export interface TestimonialItem {
    image: string;
    quote: string;
    author: string;
  }
  
  export interface HowItWorksItem {
    icon: string;
    title: string;
    image: string;
  }